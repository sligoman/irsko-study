# Blog Back Link Design

Replace the plain underlined `Zpět na blog` anchor above the blog-post hero with an inline reversed text-link: a green circular left chevron followed by the existing label. The arrow shifts left on hover, preserving the blog route and keyboard-accessible native anchor behavior.
